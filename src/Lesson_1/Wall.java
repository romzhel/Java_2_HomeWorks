package Lesson_1;

public class Wall extends Task {
    public static final int MAX_SIZE_DEFAULT = 2;

    public Wall() {
        super(MAX_SIZE_DEFAULT);
    }

    public Wall(int maxSize) {
        super(maxSize);
    }
}
